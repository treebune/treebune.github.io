# 寫在掌心的領悟 — 網站使用說明

## 目錄結構

```
treebune-final/
├── index.html          首頁（書架）
├── about.html          關於頁
├── CNAME               網域設定
├── css/
│   └── style.css       所有樣式
└── chapters/
    ├── lakecity/       湖城記事（01.html ~ 16.html）
    ├── name/           名字（01.html ~ 10.html）
    ├── story/          故事之城
    ├── yanran/         炎涼記
    ├── baitai/         百態記
    ├── limu/           黎小木上學記
    ├── hudi/           惠帝列傳
    ├── traveler/       旅行者回憶錄
    ├── animals/        動物食堂
    └── suosui/         瑣碎集
```

---

## 新增一篇文章

以任何一篇現有文章為範本，例如 `chapters/lakecity/01.html`。

1. 複製該檔案，命名為下一個編號，例如 `17.html`
2. 修改以下內容：
   - `<title>` 標籤裡的文章標題
   - `<meta name="description">` 裡的描述
   - `.chapter-series` 裡的篇數（第17篇）
   - `.chapter-title` 裡的標題
   - `.chapter-sub` 裡的日期
   - `.chapter-body` 裡的文章內容（每段用 `<p>` 包住）
   - `.chapter-nav` 裡的上一篇和下一篇連結
   - sidebar 的 `.progress-fill` 的 `width` 百分比（17/總篇數 × 100）
   - sidebar 的 `.progress-label` 的篇數顯示
   - sidebar 的 `.toc-list` 新增一個 `<li>` 項目，並把 `active` 移到新的這篇

---

## 換書脊照片

在 `index.html` 裡找到對應的書，tooltip 裡有一行：

```html
<img class="tooltip-img" src="https://images.unsplash.com/..." alt="">
```

把 URL 換成你想要的照片網址即可。

建議用 Unsplash 的圖片，格式：
```
https://images.unsplash.com/photo-XXXXXXXXX?w=360&q=60
```

---

## 新增一個系列（新書脊）

1. 在 `chapters/` 下新建一個資料夾，例如 `chapters/newcat/`
2. 在裡面建立 `01.html`，複製任何一篇文章頁面修改
3. 在 `index.html` 的書架區塊複製一個 `.book` 區塊，修改：
   - `onclick` 的連結路徑
   - tooltip 的標題、描述、狀態
   - `book-spine` 的背景色、文字色、寬度、高度、書名、篇數
4. 在 `css/style.css` 不需要改任何東西

---

## 接上 treebune.cc 網域（GoDaddy）

1. 先確認網站在 `treebune.github.io` 正常顯示
2. 進入 GoDaddy → 網域 → treebune.cc → DNS 管理
3. 新增以下 A 記錄（指向 GitHub Pages）：
   ```
   185.199.108.153
   185.199.109.153
   185.199.110.153
   185.199.111.153
   ```
4. 新增 CNAME 記錄：
   ```
   名稱：www
   值：treebune.github.io
   ```
5. 回到 GitHub repository → Settings → Pages → Custom domain 填入 `www.treebune.cc`
6. 等待 DNS 生效（約 24 小時內）

---

## Google Analytics

追蹤碼已內建在每個頁面，ID 為 `G-DBTLEKLXEG`，無需額外設定。
